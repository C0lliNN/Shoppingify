import React from 'react';
import styled from 'styled-components';

const StyledItems = styled.div`
  height: 100%;
  background-color: blue;
`;

function Items() {
  return <StyledItems>Items</StyledItems>;
}

export default Items;
